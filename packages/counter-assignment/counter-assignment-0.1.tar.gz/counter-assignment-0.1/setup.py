from setuptools import setup, find_packages

setup(
    name='counter-assignment',
    version='0.1',  # Increment the version
    packages=find_packages(),
    install_requires=[
        'Flask',
        # Other dependencies
    ],
)

