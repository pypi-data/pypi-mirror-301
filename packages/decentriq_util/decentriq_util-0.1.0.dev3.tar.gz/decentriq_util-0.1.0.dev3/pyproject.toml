[tool.poetry]
name = "decentriq_util"
version = "0.1.0-dev3"
description = "Utility functions for Python code run in Decentriq workers"
authors = ["hafenr <robin.hafen@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
protobuf = "^4.21"
pandas = ">=1.4.4"
pyspark = "^3.4.0"
psutil = "^5.9.6"
pyarrow = "^14.0.0"

[tool.poetry.dev-dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
