# This file was generated by liblab | https://liblab.com/

from enum import Enum

from .utils.base_model import BaseModel
from .utils.json_map import JsonMap


class PosterEditDefault(Enum):
    """An enumeration representing different categories.

    :cvar TRUE: "true"
    :vartype TRUE: str
    """

    TRUE = "true"

    def list():
        """Lists all category values.

        :return: A list of all category values.
        :rtype: list
        """
        return list(map(lambda x: x.value, PosterEditDefault._member_map_.values()))


@JsonMap({})
class PosterEdit(BaseModel):
    """PosterEdit

    :param default: If the poster is the default one., defaults to None
    :type default: PosterEditDefault, optional
    """

    def __init__(self, default: PosterEditDefault = None):
        """PosterEdit

        :param default: If the poster is the default one., defaults to None
        :type default: PosterEditDefault, optional
        """
        if default is not None:
            self.default = self._enum_matching(
                default, PosterEditDefault.list(), "default"
            )
