# This file was generated by liblab | https://liblab.com/

from ..models import EngagementStats, PerformanceStatsResponse
from ..models.utils.cast_models import cast_models
from ..net.transport.serializer import Serializer
from .utils.base_service import BaseService
from .utils.validator import Validator


class StatsService(BaseService):

    @cast_models
    def get_engagement_stats(
        self, project_id: int, video_id: int, start_date: str, end_date: str
    ) -> EngagementStats:
        """Get engagement stats for a specific video.

        :param project_id: The ID of the project.
        :type project_id: int
        :param video_id: ID of the video.
        :type video_id: int
        :param start_date: The start date for the period that you want analytics for.
        :type start_date: str
        :param end_date: The end date for the period that you want analytics for.
        :type end_date: str
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: OK
        :rtype: EngagementStats
        """

        Validator(int).min(1).max(9223372036854776000).validate(project_id)
        Validator(int).min(1).max(9223372036854776000).validate(video_id)
        Validator(str).validate(start_date)
        Validator(str).validate(end_date)

        serialized_request = (
            Serializer(
                f"{self.base_url}/api/v1/project/{{project_id}}/stats/engagement/video/{{video_id}}",
                self.get_default_headers(),
            )
            .add_path("project_id", project_id)
            .add_path("video_id", video_id)
            .add_query("start_date", start_date)
            .add_query("end_date", end_date)
            .serialize()
            .set_method("GET")
        )

        response = self.send_request(serialized_request)
        return EngagementStats._unmap(response)

    @cast_models
    def get_performance_stats(
        self, project_id: int, start_date: str, end_date: str, video_id: int = None
    ) -> PerformanceStatsResponse:
        """Get performance stats about the project or a single video.

        :param project_id: The ID of the project.
        :type project_id: int
        :param start_date: The start date for the period that you want analytics for.
        :type start_date: str
        :param end_date: The end date for the period that you want analytics for.
        :type end_date: str
        :param video_id: Filter by video_id., defaults to None
        :type video_id: int, optional
        ...
        :raises RequestError: Raised when a request fails, with optional HTTP status code and details.
        ...
        :return: OK
        :rtype: PerformanceStatsResponse
        """

        Validator(int).min(1).max(9223372036854776000).validate(project_id)
        Validator(str).validate(start_date)
        Validator(str).validate(end_date)
        Validator(int).is_optional().min(1).max(9223372036854776000).validate(video_id)

        serialized_request = (
            Serializer(
                f"{self.base_url}/api/v1/project/{{project_id}}/stats/performance",
                self.get_default_headers(),
            )
            .add_path("project_id", project_id)
            .add_query("video_id", video_id)
            .add_query("start_date", start_date)
            .add_query("end_date", end_date)
            .serialize()
            .set_method("GET")
        )

        response = self.send_request(serialized_request)
        return PerformanceStatsResponse._unmap(response)
