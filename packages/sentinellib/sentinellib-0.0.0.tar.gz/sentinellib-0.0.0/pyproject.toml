[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sentinellib"
description = "Placeholder for potential addtion of module to Python's stddlib."
authors = [{name = "Tal Einat", email = "tal@python.org"}]
license = { text = "PSF-2.0" }
dynamic = ["version"]
