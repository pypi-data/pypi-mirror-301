[tool.poetry]
name = "and-project"
version = "0.3.0"
description = "En test"
authors = ["AndreasB"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.scripts]
andcommand = "and_project.andy:return_hello"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
