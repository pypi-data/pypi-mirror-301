[project]
# UPDATE THIS BEFORE PUBLISHING
name = "autora-experimentalist-inequality"
description = "AutoRA Inequality Experimentalist"
authors = [
    { name = "Chad C. Williams", email = "cwilliams.uca@gmail.com" },
    { name = "Younes Strittmatter", email = "younes_strittmatter@brown.com" }
]
dynamic = ["version"]

readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8,<4"

# ADD NEW DEPENDENCIES HERE
dependencies = [
    "autora-core>=3.1.0",
]

[project.optional-dependencies]
dev = [
    "autora-core[dev]"
]

[project.urls]
homepage = "http://www.empiricalresearch.ai"
repository = "https://github.com/AutoResearch/autora-experimentalist-inequality"
documentation = "https://autoresearch.github.io/autora/"

[build-system]
requires = ["setuptools", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
