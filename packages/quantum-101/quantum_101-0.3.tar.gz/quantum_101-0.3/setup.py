from setuptools import setup, find_packages

setup(
    name='quantum_101',
    version='0.3',
    description='package tentang operasi bilangan kuantum',
    author='MegIv',
    packages=find_packages(),
    install_requires=[
        # Dependencies anda

    ],
)