[tool.poetry]
name = "imagecor"
version = "0.1.0"
description = "Image processing utilities for Markdown files"
authors = ["Your Name <your.email@example.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "imagecor"}]

[tool.poetry.dependencies]
python = ">=3.9,<4.0"
requests = "^2.32.3"
pillow = "^10.4.0"
markdown = "^3.7"
beautifulsoup4 = "^4.12.3"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
black = "^24.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"