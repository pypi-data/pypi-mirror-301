#!/usr/bin/env bash

# A script for running mypy, 
# with all its dependencies installed.

set -o errexit

# Change directory to the project root directory.
cd "$(dirname "$0")"

# Run on all files under JF Ingest
# use PDM to ensure version we are running
pdm run mypy jf_ingest